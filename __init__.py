# -*- coding: utf-8 -*-
from SwiftUtils.SwiftUtils import *

# NOTE: __init__.py is only required for packages; standalone files can serve as modules

#__all__ = [ os.path.basename(f)[:-3] for f in glob.glob(os.path.dirname(__file__)+"/*.py")]


def main():
	# Initialize the SwiftUtils module
	pass


if __name__ == '__main__':
	main()